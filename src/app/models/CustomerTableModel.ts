export interface CustomerTableModel {
  pricePaused?: string
  paused?: boolean
  startTime?: number
  isActive?: boolean
  counter?: number
  price: string
  timer: string
  status: number
  timerPaused?: number
}
